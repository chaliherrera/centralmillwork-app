# Backend URL — leave empty in local dev (Vite proxy handles /api → localhost:4000)
# Set to your Railway backend URL in production:
# VITE_BACKEND_URL=https://centralmillwork-app-production.up.railway.app
VITE_BACKEND_URL=
